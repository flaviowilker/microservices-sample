spring:
  application:
    name: estados-service
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config-service
      fail-fast: true

eureka:
  client:
    serviceUrl:
      defaultZone: https://localhost:8071/eureka/
